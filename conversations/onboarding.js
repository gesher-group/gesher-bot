const Botkit = require('botkit')
// const os = require('os')
const controller = Botkit.slackbot({ debug: false })
// const bot = controller.spawn({
  // token: process.env.SLACK_BOT_TOKEN
// }).startRTM()
console.log('controller'[cotroller.hears])

// module.exports {
  // funcName: funcName
// }
